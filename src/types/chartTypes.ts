export type ChartData = {
  dateTimeArray: string[];
  idArray: string[];
  areaArray: number[];
  barArray: number[];
};

export type ItemData = {
  id: string;
  value_area: number;
  value_bar: number;
};
